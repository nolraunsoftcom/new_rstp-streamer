# 8시간 직결 능동 스트레스 소크 — 결과 보고서

**일시:** 2026-06-14 00:27:57 ~ 08:28:25 (8시간 완주)
**대상:** main 빌드(HEAD `90a4974`, M3 안정성 수정 포함, **M4 relay 코드 없음 = 순수 직결**)
**소스:** 직결 1채널 `rtsp://169.254.4.1:8900/live` (VOXL2, H.264 640×480) — **mediamtx relay 제외**
**환경:** macOS, `caffeinate`로 슬립 방지. 장비 READ-ONLY(진단만).
**로그:** `logs/soak8h-20260614-0027/` (summary.txt, viewer.log, device.log, rss.log, recs/, snaps/)

---

## 결론: ✅ PASS

직결 단독으로 8시간 능동 스트레스(시청·녹화·스냅샷 + 반복 껐다키고 + 녹화 중 강제종료)를 **크래시 0·누수 0·끊김 100% 자동복구**로 통과. 손상으로 집계된 2개는 실제 촬영분 손실이 아닌 churn 부산물(아래 §4).

---

## 1. 테스트 구성 (지속 ↔ churn 교대, 10라운드)

| 라운드 | 유형 | 내용 |
|---|---|---|
| 1·3·5·7·9 | **지속(각 70분)** | 1회 기동 연속 `--connect --auto-record --snapshot-every=30` — 장시간 누수/녹화 롤오버/스냅샷 |
| 2·4·6·8·10 | **churn(각 25분)** | 빠른 앱 껐다키고 반복(총 53회), 모드 회전(시청/녹화토글/자동녹화), **일부 녹화 중 강제종료** |

총 **앱 기동 58회**(지속 5 + churn 53).

## 2. 안정성 — 크래시/누수

| 지표 | 결과 | 판정 |
|---|---|---|
| **비정상 종료(크래시)** | **0 / 58 기동** | ✅ |
| 로그 crash 흔적 | 0 | ✅ |
| **RSS 누수** | 시작 227 / 끝 241 / 최대 265MB, **전반¼ 219 vs 후반¼ 218MB** (458샘플/8h) | ✅ **완전 평탄** |
| CPU | 평균 78% / 최대 150%(순간) | ✅ |

8시간 RSS가 전·후반 동일(219↔218MB) — **메모리 누수 없음**이 장시간으로 입증됨.

## 3. 끊김 복구 (자연 끊김 = 장비측 NoPackets)

| 지표 | 결과 |
|---|---|
| **Stalled(자연 끊김)** | **22건** (~2.75회/시간) |
| **자동 복구(presented 후속)** | **22 / 22 (100%)** |
| 복구 시간 | 평균 **9.7s** (9.2~10.1s) |
| **Failed(영구정지)** | **0** |

모든 끊김이 사유 `NoPackets`(장비가 잠깐 패킷 송출 중단). viewer가 매번 `Stalled→Connecting→SessionOpen→presented`로 ~9.7초 내 무개입 복구. **영구 실패 0**.

## 4. 녹화 무결성

| 지표 | 결과 |
|---|---|
| 세그먼트 총수 | **179** (2.7GB) |
| **재생가능** | **177** |
| 손상·빈 | 2 |
| 0바이트 | 0 |

**손상 2개 규명:** 둘 다 **542바이트(MKV 헤더만, 영상 패킷 0개)**, 둘 다 churn 라운드(01:47, 03:25)에서 발생. 원인 = **녹화 시작 직후 첫 키프레임이 기록되기 전에 앱을 강제종료(껐다키고)** → 헤더만 남은 빈 파일. **실제 촬영분 손실 아님**(아직 프레임이 쓰이기 전). 정상 종료/롤오버 세그먼트 177개는 전부 재생가능.
> 개선 여지(저심각도): 첫 키프레임 전까지 `avio_open`을 지연하면 이 헤더-only 부산물 자체를 없앨 수 있음. 필수 아님.

## 5. 스냅샷 무결성

| 지표 | 결과 |
|---|---|
| **정상** | **928 / 928** ✅ |
| 손상 | 0 |

8시간 동안 디코딩 원본 PNG 928장 전부 정상.

## 6. 장비 거동 — 직결의 구조적 churn (M4 relay 정당성)

장비 로그상 **소스 파이프 닫음(클라이언트 0): 81회 / 새 클라이언트 연결: 80회**.
직결 모드에서는 viewer가 유일한 클라이언트라, 끊김(22) + churn 재시작(53) + 기타마다 **장비가 인코더 소스 파이프를 close→reopen**한다(`no more rtsp clients, closing source pipe intentionally`). 이는 프로젝트 초기부터의 **인코더 wedge/유령세션 위험 구간**이며, viewer는 8시간 내내 이를 견뎌냈다(reopen 실패/wedge 0).
> **M4 MediaMTX relay의 존재 이유가 이 데이터로 재확인됨**: relay가 장비에 상시 1연결을 유지하면 "클라이언트 0"이 사라져 이 81회의 close/reopen이 0이 된다. 직결은 견고하지만 장비를 거칠게 다루고, relay는 장비를 보호한다.

## 7. 종합 판정

| 항목 | 결과 |
|---|---|
| 8h 무인 안정성(누수/크래시) | ✅ PASS (누수 0, 크래시 0) |
| 자연 끊김 자동복구 | ✅ PASS (22/22, ~9.7s, Failed 0) |
| 껐다키고 스트레스(58 기동) | ✅ PASS (전부 재연결, 크래시 0) |
| 녹화 무결성 | ✅ PASS (177/177 실세그먼트, 손상 2는 churn 헤더-only 부산물) |
| 스냅샷 무결성 | ✅ PASS (928/928) |
| 장비 거친동작 내성 | ✅ PASS (close/reopen 81회 견딤) |

**직결 경로(relay 제외)는 8시간 능동 스트레스를 무인 통과.** 핸드헬드 카메라 실운용(~12h)의 핵심 시나리오(장시간 안정성·장애 자동복구·증거수집)를 직결만으로도 만족.

## 8. 후속
- (저심각도) 녹화: 첫 키프레임 전 강제종료 시 헤더-only 빈 파일 → avio_open 지연으로 제거 가능.
- M4 relay: macOS launchd link-local TCC 이슈(`docs/m4-acceptance.md`), Windows(메인) 실검증, 72h 소크 + 전원차단 주입.
- 본 소크가 보여준 장비 close/reopen 81회는 relay 도입의 직접 근거.
