# M1~M4 통합 1시간 검증 — 결과 보고서

**일시:** 2026-06-14 15:04~16:06 (1시간 완주). **결과: ✅ 전 마일스톤 통합 PASS.**
**대상:** m4-relay 빌드(M3 안정성 수정 + M4 relay + 저비용 2건 + 멱등 수정 전부 포함).
**구성:** 10채널 — **장비 1 (relay 경유, M4) + 합성 9 (직결, 별도 mediamtx 8654, M2)**. 전 채널 녹화 + 스냅샷.
**단계:** [지속 20m]→[viewer churn 15m]→[장애주입 15m: relay 중단/재개 + 합성 cam3 kill/restore]→[마무리 10m]. caffeinate.
**도구:** `ops/m1m4-stress-1h.sh`. **로그:** `logs/m1m4-20260614-1504/`

---

## 종합 판정: ✅ PASS (M1·M2·M3·M4 동시 동작 검증)

| 마일스톤 | 검증 항목 | 결과 |
|---|---|---|
| **M1** | 연결·HW디코드·zero-copy·재연결 복구 | first frame 123누적 / **Failed 0** / 비정상종료 0 |
| **M2** | 10채널 멀티·채널 독립성·성능 | 10채널 동시 / 독립성 ✅(아래) / CPU 평균105%·최대139% / RSS 평탄 |
| **M3** | 10채널 녹화·스냅샷·재연결 생존 | **녹화 193 재생가능/손상 0, 고유채널 10** / **스냅샷 1160 정상** |
| **M4** | relay 보호막·중단복구·진단 | **churn 9회 → 장비 close +0**(보호막) / relay 중단→복구 / RelayDown 진단 32 |
| 공통 | 누수·크래시 | **RSS 전반¼ 227 ↔ 후반¼ 187MB(누수0)·크래시 0** |

## 세부

### M4 — relay 보호막 (핵심)
- **viewer 9회 껐다키고(churn) → 장비 소스파이프 close/reopen +0.** 멱등 수정(`3191dfb`) 덕에 viewer 재시작이 relay를 건드리지 않아 장비 세션 무중단. (직결 8h=81회 / 멱등 수정 전 1h=재시작과 1:1 → 수정 후 **0**.)
- 장비 close 총 4회 = 콜드스타트 + 장애주입(relay 의도적 중단/재개) 기인, **churn 구간 0**.
- relay 중단(bootout)→viewer RelayDown 진단(32)→재개→hasSource=yes 자동 복구. Failed 0.

### M2 — 채널 독립성
- 10채널 각 1시간 녹화: 장비 21 / 합성1~9 각 19~20 세그먼트로 **균등**.
- 장애주입에서 **합성 cam3만 kill** → syn3 재연결 후 20 세그먼트 유지, **타 9채널 무영향**(녹화 연속). relay 중단 시에도 **합성 9채널은 무영향**(독립성 확인).

### M3 — 녹화/스냅샷
- 10채널 동시 녹화 1시간: **193 세그먼트 전부 재생가능, 손상 0**(Fix A 헤더-only 제거 실동작 — churn 강제종료 다수에도 0).
- 디코드원본 스냅샷 **1160장 전부 정상**.

### M1 — 연결/복구
- 전 채널 HW디코드+NV12 zero-copy, 재연결·stall 전부 자동복구, **Failed 0**.

## 비고
- 이번 1h는 장비 wedge 없이 완주(오전 1차 시도의 일시 wedge는 장비측 이슈, 휴식 후 회복). 장비측 watchdog(v3) 재배포는 장기 무인 운영 권장.
- CPU 최대 139%(10채널 순간) — zero-copy 경로 기준 수용 범위.

## 결론
**M1~M4가 한 런에서 통합 동작함을 1시간 실기기로 입증.** relay 보호막(멱등 수정 후)·10채널 멀티·전채널 녹화/스냅샷·채널 독립성·장애 자동복구 전부 PASS. 남은 것은 Windows(메인) 실검증, 72h 소크, 장비 watchdog 재배포.
